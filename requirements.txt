torch>=2.0.0
torchvision
numpy
matplotlib
pillow
tqdm
scikit-learn
opencv-python
pandas
einops                      # For tensor rearrangement
omegaconf                   # For config management
hydra-core                  # For experiment configs
pytorch-lightning           # Optional, for cleaner training loops
wandb                       # For experiment tracking (optional)
scipy
ftfy                        # For text cleanup (if using CLIP)
transformers                # For text-image models (if used later)
